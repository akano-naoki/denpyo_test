<%= form_for(@shop_department) do |f| %>
  <% if @shop_department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_department.errors.count, "error") %> prohibited this shop_department from being saved:</h2>

      <ul>
      <% @shop_department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
